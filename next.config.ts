import type { NextConfig } from "next";

const nextConfig = {
  images: {
    domains: ["frqouslfjppnpcpgrhjd.supabase.co", "ui-avatars.com", "res.cloudinary.com"],
  },
};

export default nextConfig;
